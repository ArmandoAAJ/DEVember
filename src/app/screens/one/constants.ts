const DAYS = [...Array(24)].map((_, i) => i + 1);

enum EnumDays {
  'one',
  'two',
  'three',
  'four',
  'five',
  'six',
  'seven',
  'eight',
}

export const CONSTANTS = {
  DAYS,
  EnumDays,
};
